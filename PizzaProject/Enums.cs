/// <summary>
/// It is a special "class" that represents a group of Menu constants (unchangeable/read-only variables)
/// </summary>

namespace PizzaProject
{
    public enum TypeOfFood
    {
        PizzaType,
        DessertType,
        BeverageType
    }

    public enum TypeOfPizza
    {
        Margherita,
        Hawaiian,
        MeatLover
    }

    public enum PizzaSize
    {
        Small,
        Medium,
        Large
    }

    public enum PizzaCrust
    {
        Thin,
        Thick
    }

    public enum Desserts
    {
        Tiramisu,
        Cheesecake
    }

    public enum Beverages
    {
        Water,
        Juice
    }
}